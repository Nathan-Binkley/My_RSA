#MY_RSA

Named this way because Python already has an RSA import and I wanted to avoid confusion.

The system works by putting the message to the power of the keys resulting in extremely large values that take a long time to break most of the time. All keys are prime numbers.